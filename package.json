{
  "name": "lang-media-triage-poc",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "dev:api": "pnpm --filter @media-triage/api dev",
    "dev:ui": "pnpm --filter @media-triage/ui dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  }
}
